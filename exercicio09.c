#include <stdio.h>

/*
    Escreva um programa que simule o jogo Blackjack (também chamado de 21). Inicialmente, crie a struct para  representar  as  cartas  e  um  vetor  para  armazenar  todo  o  baralho.  Faça  a  atribuição  dos  valores automaticamente. O objetivo do jogo é fazer mais pontos que o adversário (computador). Basicamente, o jogador pode retirar uma carta do baralho e o valor de cada carta (independente do naipe) é somado ao seu total (considere que A vale 1, e Q, J, K sempre valem 10). Cada vez que o jogador escolher retirar uma carta, o programa sorteia uma carta aleatória (posição do vetor) e a mostra na tela, até que o jogador escolha não mais  retirar  cartas.  Então,  o  programa  sorteia  as  cartas  para  o  computador,  trapaceando  para  que  o computador nunca estoure 21 pontos (caso em que se perde o jogo). Além de implementar a “trapaça”, você não deve permitir que a mesma carta apareça mais de uma vez.
*/



int main ()
{



    return 0;
}